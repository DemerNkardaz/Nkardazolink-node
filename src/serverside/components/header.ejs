<% 
  const { urlModes = null, userDevice, userURL, domainURL, fullURL, renderLanguage} = metaDataResponse;
  const pageUrlMode = urlModes && `${urlModes.mode ? urlModes.mode : 'default'}${urlModes.select ? urlModes.select : ''}` || 'default';
%>
<header>
  This is some header
  <%= Math.random() %>
  <%= __NK__.langs.supported %>
  <%= locale[`ru`].Nkardaz.name %>
  <img src="/shared/images/kamon_glyph.png" alt="Nkardaz kamon glyph" width="256">
</header>