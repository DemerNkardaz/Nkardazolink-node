const { JSDOM } = require('jsdom');
const { window } = new JSDOM();
const $ = require('jquery')(window);

async function handleLocaleKeys(content, callback) {
  try {

  } catch (error) {
    console.error(error);
  }
}